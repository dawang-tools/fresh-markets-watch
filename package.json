{
  "name": "fresh-markets-watch",
  "version": "0.2.0",
  "private": true,
  "description": "Daydream bounty #1 — list new AMM pairs (Base). Deploy + x402 required for payout.",
  "type": "module",
  "scripts": {
    "smoke": "tsx scripts/smoke_watch.ts",
    "start": "tsx src/server.ts",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
